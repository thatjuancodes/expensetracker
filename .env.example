# Copy this file to .env and adjust values. Only variables prefixed with VITE_ are exposed to the client.

# App metadata
VITE_APP_NAME=Expense Tracker
VITE_APP_ENV=development

# OpenAI integration (client-side demo only; prefer a server proxy in production)
# Set to true to enable the OpenAI client in the UI
VITE_USE_OPENAI=false
# The OpenAI API key used by the browser (NOT recommended for production)
# VITE_OPENAI_API_KEY=
# Default model used by the client
VITE_OPENAI_MODEL=gpt-5